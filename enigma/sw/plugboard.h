#include "globals.h"

int plugboard[LETTERS] {
    INT('F'), // A => F
    INT('K'), // B => K
    INT('S'), // C => S
    INT('J'), // D => J
    INT('E'), // E => E
    INT('A'), // F => A
    INT('T'), // G => T
    INT('H'), // H => H
    INT('Z'), // I => Z
    INT('D'), // J => D
    INT('B'), // K => B
    INT('L'), // L => L
    INT('M'), // M => M
    INT('Y'), // N => Y
    INT('V'), // O => V
    INT('Q'), // P => Q
    INT('P'), // Q => P
    INT('R'), // R => R
    INT('C'), // S => C
    INT('G'), // T => G
    INT('X'), // U => X
    INT('O'), // V => O
    INT('W'), // W => W
    INT('U'), // X => U
    INT('N'), // Y => N
    INT('I')  // Z => I
};
