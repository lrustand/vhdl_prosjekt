#include "globals.h"

int reflector[LETTERS] {
    INT('Y'), // A => Y
    INT('R'), // B => R
    INT('U'), // C => U
    INT('H'), // D => H
    INT('Q'), // E => Q
    INT('S'), // F => S
    INT('L'), // G => L
    INT('D'), // H => D
    INT('P'), // I => P
    INT('X'), // J => X
    INT('N'), // K => N
    INT('G'), // L => G
    INT('O'), // M => O
    INT('K'), // N => K
    INT('M'), // O => M
    INT('I'), // P => I
    INT('E'), // Q => E
    INT('B'), // R => B
    INT('F'), // S => F
    INT('Z'), // T => Z
    INT('C'), // U => C
    INT('W'), // V => W
    INT('V'), // W => V
    INT('J'), // X => J
    INT('A'), // Y => A
    INT('T')  // Z => T
};
